
plugins {
    id 'com.android.application'
    id 'org.jetbrains.kotlin.android'
    id 'org.jetbrains.kotlin.kapt'
    id 'com.google.dagger.hilt.android'
}

android {
    namespace "com.delivery.rider"
    compileSdk 34

    defaultConfig {
        applicationId "com.delivery.rider"
        minSdk 23
        targetSdk 34
        versionCode 100
        versionName "1.0.0"

        testInstrumentationRunner "androidx.test.runner.AndroidJUnitRunner"
        
        buildConfigField("String", "API_BASE_URL", "\"https://anomaah-auth.onrender.com/\"")
        // All services go through API Gateway for proper routing
        buildConfigField("String", "API_GATEWAY_URL", "\"https://anomaah.onrender.com/\"")
        // Status updates go through API Gateway (not direct to status service)
        buildConfigField("String", "STATUS_SERVICE_URL", "\"https://anomaah.onrender.com/\"")
        buildConfigField("String", "ORDER_SERVICE_URL", "\"https://anomaah.onrender.com/\"")
        buildConfigField("String", "TRACKING_SERVICE_URL", "\"https://anomaah.onrender.com/\"")
        buildConfigField("String", "NOTIFICATION_SERVICE_URL", "\"https://anomaah.onrender.com/\"")
        
        // Map configuration: Google Maps (primary), Mapbox (fallback)
        buildConfigField("String", "MAP_PROVIDER", "\"google\"")  // "google" or "mapbox"
        
        // Google Maps API key (get from local.properties for security)
        def googleMapsKey = project.hasProperty('GOOGLE_MAPS_API_KEY') ? project.property('GOOGLE_MAPS_API_KEY') : 'YOUR_GOOGLE_MAPS_KEY'
        buildConfigField("String", "GOOGLE_MAPS_API_KEY", "\"${googleMapsKey}\"")
        resValue("string", "google_maps_key", "${googleMapsKey}")
        
        // Mapbox access token (fallback provider)
        def mapboxToken = project.hasProperty('MAPBOX_ACCESS_TOKEN') ? project.property('MAPBOX_ACCESS_TOKEN') : ''
        buildConfigField("String", "MAPBOX_ACCESS_TOKEN", "\"${mapboxToken}\"")
        
        // For development/testing: use OSM in WebView if both Google and Mapbox fail
        buildConfigField("boolean", "USE_OSM_FALLBACK", "false")
        buildConfigField("Boolean", "DEBUG_MODE", "false")
    }

    signingConfigs {
        release {
            storeFile file(System.getenv('RIDER_KEYSTORE_PATH') ?: './rider-app.jks')
            storePassword System.getenv('RIDER_KEYSTORE_PASSWORD') ?: 'android'
            keyAlias System.getenv('RIDER_KEY_ALIAS') ?: 'rider-key'
            keyPassword System.getenv('RIDER_KEY_PASSWORD') ?: 'android'
        }
    }

    buildTypes {
        release {
            signingConfig signingConfigs.release
            minifyEnabled true
            shrinkResources true
            proguardFiles getDefaultProguardFile('proguard-android-optimize.txt'), 'proguard-rules.pro'
            debuggable false
        }
        debug {
            minifyEnabled false
            debuggable true
        }
    }

    buildToolsVersion "34.0.0"

    compileOptions {
        sourceCompatibility JavaVersion.VERSION_11
        targetCompatibility JavaVersion.VERSION_11
    }

    kotlinOptions {
        jvmTarget = '11'
    }

    buildFeatures {
        viewBinding true
        dataBinding true
        buildConfig true
    }
    
    // kapt block removed for Java 17 compatibility
    // Ensure kapt and hilt versions match Kotlin plugin
}

dependencies {
    // Core Android
    implementation 'androidx.core:core-ktx:1.10.1'
    implementation 'androidx.appcompat:appcompat:1.6.1'
    implementation 'androidx.activity:activity-ktx:1.7.2'
    implementation 'androidx.fragment:fragment-ktx:1.6.1'
    implementation 'androidx.lifecycle:lifecycle-viewmodel-ktx:2.6.1'
    implementation 'androidx.lifecycle:lifecycle-runtime-ktx:2.6.1'
    implementation 'androidx.lifecycle:lifecycle-livedata-ktx:2.6.1'
    
    // Navigation
    implementation 'androidx.navigation:navigation-fragment-ktx:2.7.1'
    implementation 'androidx.navigation:navigation-ui-ktx:2.7.1'
    
    // UI
    implementation 'com.google.android.material:material:1.9.0'
    implementation 'androidx.constraintlayout:constraintlayout:2.1.4'
    
    // Data Storage
    implementation 'androidx.datastore:datastore-preferences:1.0.0'
    implementation 'androidx.security:security-crypto:1.1.0-alpha06'
    
    // Networking
    implementation 'com.squareup.retrofit2:retrofit:2.9.0'
    implementation 'com.squareup.retrofit2:converter-gson:2.9.0'
    implementation 'com.squareup.okhttp3:okhttp:4.11.0'
    implementation 'com.squareup.okhttp3:logging-interceptor:4.11.0'
    
    // JSON
    implementation 'com.google.code.gson:gson:2.10.1'
    
    // Dependency Injection
    implementation 'com.google.dagger:hilt-android:2.48'
    kapt 'com.google.dagger:hilt-compiler:2.48'
    
    // Location & Maps
    implementation 'com.google.android.gms:play-services-location:21.0.1'
    
    // Google Maps SDK (primary map provider)
    implementation 'com.google.android.gms:play-services-maps:18.1.0'
    
    // Mapbox SDK (fallback map provider)
    implementation 'com.mapbox.maps:android:10.16.0'

    // WebSocket
    implementation 'com.neovisionaries:nv-websocket-client:2.14'

    // Firebase (Push Notifications) — enabled by default
    implementation 'com.google.firebase:firebase-messaging:23.2.1'
    
    // Image Loading
    implementation 'com.github.bumptech.glide:glide:4.15.1'
    kapt 'com.github.bumptech.glide:compiler:4.15.1'
    
    // Date/Time
    implementation 'joda-time:joda-time:2.12.5'
    
    // Testing
    testImplementation 'junit:junit:4.13.2'
    testImplementation 'org.mockito:mockito-core:5.2.0'
    testImplementation 'org.mockito.kotlin:mockito-kotlin:5.0.0'
    
    androidTestImplementation 'androidx.test.ext:junit:1.1.5'
    androidTestImplementation 'androidx.test.espresso:espresso-core:3.5.1'
}
